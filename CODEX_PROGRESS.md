# RustMoku progress

- Actual HEAD: `d273f4787db8e9b0144874fd7700579674c544aa` (`w5-w8`). W5-W8 committed; current CI/CR/W9 modifications and new book/offline/scripts are uncommitted. No hosted-CI claim for this diff.
- CI: reproduced the exact requested workspace command; sole layout regression failed. Fixed analysis nesting counter usize -> checked u8 in Position padding, preserving original 4344-byte assertion. Targeted failing test and Core analysis-turn tests passed. Reserved final exact CI rerun has NOT run.
- W5-W8 CR implemented: portable V3 scalar+auto semantic evidence and separate AVX2 target receipt; sampled/off mining, cached group tensors, precise forced-loss tags, distributed train-only scale sample; named lmr_v2_policy ablation; Native history 280/window 980. Frozen architecture 4 and semantically-reset worker scratch retained; docs corrected.
- W9 partial: empirical OpeningDatabase/versioned CLI/strict identity/D4/bounded parser/atomic publication; engine OrderOnly/BookMove without proof/TT Exact. Two-ply build/resume byte equality fixed and tested. Opening starts and empirical dataset export/import implemented; serious/large default to built-in suite + random_plies=0. Native/Arena runtime book configuration and experiment identity still pending.
- W9 disk baseline: SQLite compact stable NodeIds, bounded page cache/disk quota, PN/DN/frontier/edges, native legal/D4 replay, deterministic resume, all-legal aggregation, fresh witness verification and independent native ProofBook export check. Deterministic process shards and verified witness merge implemented; Unknown cannot refute. Remaining: integrate VCF/VCT, strict aggregate work/wall/RSS accounting, stronger adversarial fixtures and scale evidence. Old resident solver retains its 100k cap; new disk path has no node-count cap. RAM option currently means SQLite cache, not process RSS.
- W9 composition: immutable mixed source manifests preserve exact labels, proof lineage, outcomes and comparisons; train-only canonical dedupe/proof hash subsampling and production sample weights; exact opening labels reject. Four operational PowerShell scripts plus OFFLINE_INTELLIGENCE.md describe current executable commands and limitations. No long examples executed.
- Checks passed: original CI failure targeted (1), Core analysis-turn (2), LMR profile (1), OpeningDatabase (2, D4/start replay rechecked after extension), Python production (4), V3 evidence (3), native replay (1), offline storage/solve/shards (5), two-ply opening CLI/import (1), composition (2), compact (3), four script parser checks; data/book/solver debug builds and one immediate-win proof PowerShell smoke (work=1, fresh native verify). No large proof/training/Arena. Final fmt/check/clippy and final CI rerun NOT run; reserve until W9 closure.
- Native compile follow-up: added explicit English/Chinese OpeningBook result text (empirical, not proof). `cargo check -p rustmoku-native --all-targets` and the targeted localization wording regression passed.
- Next exact task: wire OpeningDatabase into Native/Arena frozen application/experiment configuration; close disk resource/VCF/VCT and missing defender/malformed-D4/shard-refutation regressions; finish remaining CR ablation/mining tests. Then run the requested final fmt/check/clippy and exact CI command once, update final risks/manual commands, stop at W9. W10/W11 remain untouched.
